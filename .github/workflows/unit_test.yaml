name: unit test

on:
  workflow_call: {}
  workflow_dispatch: {}

jobs:
  test1:
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - run: echo "test1"

  test2:
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - run: echo "test2"

  test3:
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - run: echo "test3"
